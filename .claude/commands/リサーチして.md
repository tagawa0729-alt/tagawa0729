あなたはSNSバズ情報のリサーチエージェントです。以下の手順でリサーチを実行し、全結果を `knowledge/research-knowledge.md` に追記保存してください。

作業ディレクトリ: /Users/akiharutagawa/Desktop/クロード１/
X_BEARER_TOKEN: AAAAAAAAAAAAAAAAAAAAAMeM8wEAAAAA69mjlkg27upIVTVkIRJg%2FBWb1gk%3D33R198VHpnoxq4SyrXIUwkrOXStbl5g34qrCGaEvQYbGdBxrVF

---

## ① Instagram リサーチ

### STEP 1: 美容系アカウントのリストアップ
@haruoo.biyou のフォロー一覧から美容・スキンケア系アカウントを特定する。
WebSearchで「Instagram メンズスキンケア バズ アカウント 2025」も合わせて調べ、インフルエンサーリストを作成する。

### STEP 2: 各アカウントのタイムライン確認
特定した各アカウントについて：
- 再生数10万以上のリールのみピックアップ
- そのアカウントがフォローしているアカウントも確認し、同ジャンルで伸びているアカウントを発見
- 発見した新アカウントでも同じことを繰り返す

### STEP 3: yt-dlp でダウンロード・Whisper で文字起こし
```bash
yt-dlp -x --audio-format mp3 -o "tmp/%(id)s.%(ext)s" {リールURL}
python3 -c "
import whisper; model = whisper.load_model('base')
result = model.transcribe('tmp/{file}.mp3', language='ja')
print(result['text'])
"
```

### STEP 4: 収集する情報
- キャプション全文（1行目のフックを必ず抜き出す）
- コメント欄のリアルな悩み・質問
- バズ投稿の構成パターン（問題提起→解決→CTA 等）

---

## ② X リサーチ

### 検索キーワード（各50本取得）
`スキンケア` `クリーム` `化粧水` `化粧品` `洗顔` `保湿` `毛穴` `ニキビ` `日焼け止め`

### X API v2 取得
```python
import requests
BEARER = "AAAAAAAAAAAAAAAAAAAAAMeM8wEAAAAA69mjlkg27upIVTVkIRJg%2FBWb1gk%3D33R198VHpnoxq4SyrXIUwkrOXStbl5g34qrCGaEvQYbGdBxrVF"
headers = {"Authorization": f"Bearer {BEARER}"}
for keyword in ["スキンケア","クリーム","化粧水","化粧品","洗顔","保湿","毛穴","ニキビ","日焼け止め"]:
    r = requests.get("https://api.twitter.com/2/tweets/search/recent", headers=headers,
        params={"query": f"{keyword} lang:ja -is:retweet", "max_results": 100,
                "tweet.fields": "public_metrics,text", "sort_order": "relevancy"})
    tweets = sorted(r.json().get("data",[]), key=lambda x: x["public_metrics"]["like_count"], reverse=True)[:50]
```

コメントや投稿内で気になるキーワードが出てきたら追加検索する。

### 整理する情報
1. 肌タイプごとの具体的なスキンケアのやり方
2. よくある失敗・やりがちなNG
3. 専門家（医師・研究者）の見解
4. おすすめグッズの商品名・価格
5. バズ投稿の共通ワード・文章フォーマット

---

## 保存フォーマット（knowledge/research-knowledge.md に追記）

```
## YYYY-MM-DD HH:MM リサーチ結果

### Instagram リサーチ
| アカウント名 | リールURL | 再生数 | 1行目フック | 構成パターン |
|------------|---------|-------|-----------|------------|

#### ユーザーコメントから拾った悩みワード
- 「〇〇」（〇件のコメントで言及）

### X リサーチ
#### キーワード別 バズ投稿TOP5
| キーワード | 投稿テキスト冒頭 | いいね数 | バズパターン分類 |
|----------|--------------|--------|--------------|

#### 共通点・フォーマット分析
- 肌タイプ別スキンケア:
- よくある失敗NG:
- 専門家の見解:
- おすすめグッズ（商品名・価格）:
- バズワード・フック型:

#### 追加調査したキーワード
-
```

---

## 完了条件
- Instagram: 再生数10万以上のリール最低5本を文字起こし済み
- X: 各キーワードで上位50本取得・共通点整理済み
- `knowledge/research-knowledge.md` に追記保存済み
